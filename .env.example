# =============================================================================
# Aroha Astrology Mobile — env vars (set as EAS Secrets, not committed here)
# =============================================================================
# For local dev only — production values come from EAS Secrets at build time.

EXPO_PUBLIC_API_URL=http://localhost:3001
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
